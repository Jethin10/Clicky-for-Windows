<Window x:Class="Clicky.Windows.Views.AgentResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Agent result" Width="680" Height="620"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResizeWithGrip"
        Background="#101211" Foreground="#ECEEED">
  <Window.Resources>
    <SolidColorBrush x:Key="Surface" Color="#202221" />
    <SolidColorBrush x:Key="Border" Color="#373B39" />
    <SolidColorBrush x:Key="Secondary" Color="#ADB5B2" />
    <SolidColorBrush x:Key="Tertiary" Color="#77807C" />
  </Window.Resources>
  <Grid Margin="22">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <StackPanel>
      <TextBlock Text="Agent result" FontFamily="Segoe UI Variable" FontSize="22" FontWeight="SemiBold" />
      <TextBlock x:Name="TaskText" Foreground="{StaticResource Secondary}" TextWrapping="Wrap" Margin="0,7,0,0" FontSize="12" MaxHeight="58" TextTrimming="CharacterEllipsis" />
    </StackPanel>

    <Grid Grid.Row="1" Margin="0,14,0,12">
      <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
      <TextBlock x:Name="ProviderText" Foreground="{StaticResource Tertiary}" FontSize="10" VerticalAlignment="Center" />
      <TextBlock x:Name="CompletedText" Grid.Column="1" Foreground="{StaticResource Tertiary}" FontSize="10" VerticalAlignment="Center" />
    </Grid>

    <TextBox x:Name="ResultText" Grid.Row="2" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap"
             VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
             Background="#171918" Foreground="#D9DEDC" BorderBrush="{StaticResource Border}" BorderThickness="1"
             FontFamily="Segoe UI Variable" FontSize="13" Padding="14" />

    <Grid Grid.Row="3" Margin="0,14,0,0">
      <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
      <Button Content="Copy complete result" Click="CopyResult" HorizontalAlignment="Left" Background="{StaticResource Surface}" Foreground="#60A5FA" BorderThickness="0" Padding="15,9" Cursor="Hand" />
      <Button Grid.Column="1" Content="Close" Click="CloseWindow" Background="#2563EB" Foreground="White" BorderThickness="0" Padding="18,9" FontWeight="SemiBold" Cursor="Hand" />
    </Grid>
  </Grid>
</Window>
